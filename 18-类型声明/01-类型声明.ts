// 告诉编译器存在这些类型,可以直接使用,不会报错, 但是不能赋值

// 这些类型声明就像我们在 TypeScript 中的类型标注一样，会存放着特定的类型信息，同时由于它们并不具有实际逻辑，我们可以很方便地使用类型声明来进行类型兼容性的比较、工具类型的声明与测试等等。

// declare var f1: () => void

// declare interface Foo {
//   prop: string
// }

// declare function foo(input: Foo): Foo

// declare class Foo {}
