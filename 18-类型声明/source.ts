// 源代码
const handler1 = (input: string): boolean => {
  return input.length > 5
}

interface Foo2 {
  name: string
  age: number
}

const foo2: Foo2 = {
  name: 'faiz-gear',
  age: 18
}

class FooCls {
  prop!: string
}
